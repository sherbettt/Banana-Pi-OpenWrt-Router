## 🎨 Настройка цветного приглашения командной строки (PS1) в OpenWrt One

---

### 📌 Введение

OpenWrt One использует **BusyBox Ash** (`ash`) в качестве командной оболочки. Это упрощённая, но функциональная оболочка, которая имеет некоторые отличия от Bash.

**Особенности BusyBox Ash:**
- Не поддерживает команду `source` (используйте `.` или `source` через `mksh`/`bash`)
- Ограниченная поддержка escape-последовательностей
- Свой синтаксис для цветов

---

### 🎨 Синтаксис цветов в PS1 для Ash

#### Стандартный синтаксис (работает в Ash):
```bash
\033[1;32m  # ярко-зелёный
\033[1;33m  # ярко-жёлтый
\033[1;34m  # ярко-синий
\033[1;35m  # ярко-фиолетовый
\033[1;36m  # ярко-голубой
\033[1;31m  # ярко-красный
\033[0m     # сброс цвета
```

#### Альтернативный синтаксис (через \e):
```bash
\e[1;32m   # ярко-зелёный
\e[1;33m   # ярко-жёлтый
\e[0m      # сброс цвета
```

---

### 🔧 Настройка .profile

Файл `.profile` находится в домашней директории root:
```bash
/root/.profile
```

#### Базовое цветное приглашение:
```bash
export PS1='\033[1;32m\u\033[1;33m@\033[1;35m\h\033[0;36m:\033[1;34m\w\033[1;31m\$ \033[0m'
```

**Результат:** `root@OpenWrt:~#` (разноцветный)

#### С отображением кода ошибки последней команды:
```bash
export PS1='\033[1;32m\u\033[1;33m@\033[1;35m\h\033[1;31m($?)\033[0;36m:\033[1;34m\w\033[1;31m\$ \033[0m'
```

**Результат:** `root(0)@OpenWrt:~#` — число в скобках показывает код возврата последней команды (0 = успех).

#### Расшифровка специальных символов:

| Символ | Значение |
|--------|----------|
| `\u` | имя текущего пользователя (root) |
| `\h` | имя хоста (OpenWrt) |
| `\w` | текущая рабочая директория |
| `\$` | `#` для root, `$` для обычного пользователя |
| `\$?` | код возврата последней команды |

---

### 📝 Пошаговая настройка

#### 1. Установка Midnight Commander (удобный редактор):
```bash
apk add mc
```

#### 2. Редактируем .profile:
```bash
mcedit .profile
```

#### 3. Добавляем цветное приглашение:
```bash
# Цветное приглашение командной строки
export PS1='\033[1;32m\u\033[1;33m@\033[1;35m\h\033[1;31m($?)\033[0;36m:\033[1;34m\w\033[1;31m\$ \033[0m'
```

#### 4. Добавляем полезные алиасы:
```bash
# Полезные алиасы
alias cls='clear'
alias ls='ls --color=always'
alias ll='ls -alF --color=always'
alias la='ls -A --color=always'
```

#### 5. Применяем изменения:
```bash
. .profile
```

**Важно:** В BusyBox Ash вместо `source .profile` нужно использовать `. .profile`

#### 6. Проверяем:
```bash
echo $PS1
```

---

### 🎯 Готовый рабочий .profile

```bash
# Цветное приглашение командной строки для BusyBox Ash
export PS1='\033[1;32m\u\033[1;33m@\033[1;35m\h\033[1;31m($?)\033[0;36m:\033[1;34m\w\033[1;31m\$ \033[0m'

# Полезные алиасы
alias cls='clear'
alias ls='ls --color=always'
alias ll='ls -alF --color=always'
alias la='ls -A --color=always'

# История команд
export HISTFILESIZE=10000
export HISTSIZE=10000
```

---

### 🚨 Частые проблемы и решения

| Проблема | Причина | Решение |
|----------|---------|---------|
| `source: not found` | BusyBox Ash не поддерживает `source` | Используйте `. .profile` |
| Цвета не работают | Неправильный синтаксис | Используйте `\033[1;32m` вместо `\[\e[1;32m\]` |
| Мусор в строке (`;3M;3m`) | Лишние символы в .profile | Удалите всё лишнее, оставьте только export PS1=... |
| Алиас `ll` не работает | Рекурсивный вызов | Не называйте алиас как существующую команду |

---

### 📊 Таблица цветов ANSI

| Цвет | Обычный | Яркий |
|------|---------|-------|
| Чёрный | `\033[0;30m` | `\033[1;30m` |
| Красный | `\033[0;31m` | `\033[1;31m` |
| Зелёный | `\033[0;32m` | `\033[1;32m` |
| Жёлтый | `\033[0;33m` | `\033[1;33m` |
| Синий | `\033[0;34m` | `\033[1;34m` |
| Фиолетовый | `\033[0;35m` | `\033[1;35m` |
| Голубой | `\033[0;36m` | `\033[1;36m` |
| Белый | `\033[0;37m` | `\033[1;37m` |
| Сброс | `\033[0m` | — |

---

### 🔄 Если хотите вернуть стандартное приглашение

```bash
mcedit .profile
# Закомментируйте или удалите строку с export PS1
# Или установите стандартное значение:
export PS1='\u@\h:\w\$ '

. .profile
```

---

### 💡 Дополнительные советы

1. **Проверка поддержки цвета в терминале:**
   ```bash
   echo -e "\033[1;32mGreen text\033[0m"
   ```

2. **Просмотр текущего PS1:**
   ```bash
   echo $PS1 | cat -A
   ```

3. **Временная смена PS1 (до перезагрузки):**
   ```bash
   export PS1='\033[1;35m\u\033[0m$ '
   ```

---

### ✅ Итог

После настройки вы получите информативное цветное приглашение:
- **root** — зелёный
- **@** — жёлтый
- **OpenWrt** — фиолетовый
- **(0)** — красный (код возврата)
- **:** — голубой
- **~** — синий (текущая директория)
- **#** — красный (приглашение root)

**Цвета делают работу в консоли удобнее и информативнее!** 🎨

