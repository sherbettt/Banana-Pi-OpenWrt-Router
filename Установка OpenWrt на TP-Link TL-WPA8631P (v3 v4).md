# Полное руководство: Установка OpenWrt на TP-Link TL-WPA8631P (v3/v4)

## Содержание

1.  [Что вы получите в итоге](#что-вы-получите-в-итоге)
2.  [Что вам понадобится](#что-вам-понадобится)
3.  [Шаг 1: Определение версии устройства](#шаг-1-определение-версии-устройства)
4.  [Шаг 2: Подготовка — обновление стоковой прошивки TP-Link](#шаг-2-подготовка--обновление-стоковой-прошивки-tp-link)
5.  [Шаг 3: Первое подключение к TL-WPA8631P (ДО прошивки)](#шаг-3-первое-подключение-к-tl-wpa8631p-до-прошивки)
6.  [Шаг 4: Скачивание правильной прошивки OpenWrt](#шаг-4-скачивание-правильной-прошивки-openwrt)
7.  [Шаг 5: Прошивка OpenWrt через веб-интерфейс](#шаг-5-прошивка-openwrt-через-веб-интерфейс)
8.  [Шаг 6: Что делать, если прошивка не принимается (ошибка имени файла)](#шаг-6-что-делать-если-прошивка-не-принимается-ошибка-имени-файла)
9.  [Шаг 7: Первый вход в OpenWrt (ПОСЛЕ прошивки) — РЕШЕНИЕ КОНФЛИКТА IP](#шаг-7-первый-вход-в-openwrt-после-прошивки--решение-конфликта-ip)
10. [Шаг 8: Базовая настройка OpenWrt](#шаг-8-базовая-настройка-openwrt)
11. [Шаг 9: Восстановление заводской прошивки (если понадобится)](#шаг-9-восстановление-заводской-прошивки-если-понадобится)
12. [Часто задаваемые вопросы (FAQ)](#часто-задаваемые-вопросы-faq)

---

## Что вы получите в итоге

После прошивки ваш TP-Link TL-WPA8631P превратится из простого Powerline-адаптера в полноценное сетевое устройство под управлением OpenWrt.

**Что вы сможете делать:**
- Полный контроль через SSH и веб-интерфейс LuCI
- Устанавливать любые приложения (`zapret2`, AdGuard Home, WireGuard, Samba, Torrent-клиент, `open-plc-utils` и сотни других)
- Тонкая настройка Wi-Fi (мощность, каналы, MESH, roaming)
- Гибкие сценарии маршрутизации и фильтрации трафика
- Объединение в единую сеть с вашим основным роутером на OpenWrt (BPI-R4, OpenWRT One или любым другим)

**Что сохраняется:**
- Базовая функциональность PowerLine (HomePlug AV) — передача данных по электрической сети

---

## Что вам понадобится

- **Компьютер с браузером** (Firefox, Chrome, Edge)
- **Патч-корд (Ethernet-кабель)** — категория 5e или выше, **обязательно для настройки**
- **Доступ к розетке 230В** для питания адаптера
- **Базовое понимание сетевых настроек** (как менять IP-адрес на компьютере)

---

## Шаг 1: Определение версии устройства

Это самый важный шаг. От него зависит, какую прошивку скачивать.

1.  **Отключите адаптер от розетки.**
2.  **Переверните его и найдите наклейку.**
3.  **Найдите строку с надписью "Hardware Version" или "Ver".**

Примеры:
- `TL-WPA8631P v3` — версия 3
- `TL-WPA8631P v4` — версия 4 (может быть также `v4.6`, `v4.60`)

---

## Шаг 2: Подготовка — обновление стоковой прошивки TP-Link

Этот шаг не обязателен, но **рекомендован**. Он обновляет микрокод чипа PowerLine и загрузчика, что снижает риск проблем после перехода на OpenWrt.

### Где взять последнюю прошивку:

1.  Перейдите на глобальный сайт поддержки TP-Link: [https://www.tp-link.com/us/support/download/](https://www.tp-link.com/us/support/download/)
2.  В поиске введите `TL-WPA8631P`
3.  Выберите вашу аппаратную версию (`v3` или `v4.6`)
4.  Скачайте раздел **Firmware** (самый свежий файл `.bin`)

### Как обновить:

1.  Подключитесь к адаптеру (см. [Шаг 3](#шаг-3-первое-подключение-к-tl-wpa8631p-до-прошивки))
2.  В веб-интерфейсе перейдите в `System Tools → Firmware Upgrade`
3.  Выберите скачанный файл
4.  Нажмите `Upgrade` и дождитесь перезагрузки (3-5 минут)

---

## Шаг 3: Первое подключение к TL-WPA8631P (ДО прошивки)

### Проблема: ваш роутер уже использует 192.168.1.1

Это нормально. Адаптер TP-Link по умолчанию тоже использует этот адрес, но **вы не можете иметь два устройства с одинаковым IP в одной сети**.

**Решение: подключитесь к адаптеру НАПРЯМУЮ, минуя ваш основной роутер.**

### Инструкция:

1.  **Отключите адаптер от розетки.**
2.  **Отключите компьютер от вашего основного роутера** (выньте кабель из LAN-порта или отключитесь от Wi-Fi).
3.  **Подключите компьютер к адаптеру напрямую**:
    - Вставьте адаптер в розетку 230В.
    - Соедините компьютер и любой LAN-порт адаптера **патч-кордом**.
    - Не используйте Wi-Fi для этого шага!
4.  **Настройте на компьютере статический IP-адрес** в одной сети с адаптером:

    **Windows:**
    - `Панель управления` → `Центр управления сетями` → `Изменение параметров адаптера`
    - ПКМ по вашему Ethernet → `Свойства` → `IP версии 4 (TCP/IPv4)` → `Свойства`
    - Выберите `Использовать следующий IP-адрес`:
      - IP-адрес: `192.168.1.10`
      - Маска подсети: `255.255.255.0`
      - Основной шлюз: `192.168.1.1` (или оставьте пустым)
    - Нажмите `ОК`

    **Linux/macOS:**
    ```bash
    sudo ip addr add 192.168.1.10/24 dev eth0  # замените eth0 на ваш интерфейс
    ```

5.  **Откройте браузер** и введите адрес: **`http://192.168.1.1`** (или `http://tplinkplc.net`, если работает).
6.  **Создайте пароль администратора** при первом входе. Запомните его!

---

## Шаг 4: Скачивание правильной прошивки OpenWrt

**Для версии 23.05.6 (рекомендуемая стабильная версия):**

Используйте эти прямые ссылки (на основе данных из официального репозитория):

- **Для TL-WPA8631P v3:**
  `https://downloads.openwrt.org/releases/23.05.6/targets/ramips/mt7621/openwrt-23.05.6-ramips-mt7621-tplink_tl-wpa8631p-v3-squashfs-factory.bin`

- **Для TL-WPA8631P v4:**
  Используйте **тот же файл, что и для v3** (это официально поддерживаемый способ). Ссылка та же, что выше.

**Альтернативный вариант — версия 22.03.7:**
Если по какой-то причине 23.05.6 не подойдёт, используйте:
`https://downloads.openwrt.org/releases/22.03.7/targets/ramips/mt7621/openwrt-22.03.7-ramips-mt7621-tplink_tl-wpa8631p-v3-squashfs-factory.bin`

**Почему именно `factory.bin`?**
Этот файл предназначен для **первой прошивки** поверх заводской TP-Link. Файлы `initramfs-kernel.bin` и `sysupgrade.bin` для этого не подходят.

---

## Шаг 5: Прошивка OpenWrt через веб-интерфейс

1.  **Убедитесь, что вы всё еще подключены напрямую** к адаптеру (как в Шаге 3), а не к роутеру.
2.  **Перейдите в раздел:** `System Tools → Firmware Upgrade`.
3.  **Нажмите "Browse"** (или "Choose File") и выберите скачанный файл `...-factory.bin`.
4.  **Нажмите "Upgrade"**.
5.  **Подтвердите действие**, если появится запрос.
6.  **Не выключайте и не перезагружайте адаптер** во время процесса! Индикаторы будут мигать — это нормально.
7.  **Дождитесь автоматической перезагрузки.** Обычно это занимает 3-5 минут.

---

## Шаг 6: Что делать, если прошивка не принимается (ошибка имени файла)

**Проблема:** После нажатия "Upgrade" появляется ошибка `"Wrong file!"` или `"Incorrect firmware"`.

**Причина:** Веб-интерфейс TP-Link проверяет имя файла и ожидает видеть имя, совпадающее с оригинальной прошивкой TP-Link.

**Решение — переименовать файл:**

1.  **Узнайте точное имя текущей прошивки** (обычно оно отображается где-то на странице Firmware Upgrade).
2.  **Переименуйте** скачанный файл `openwrt-23.05.6-...-factory.bin` в **точности** как оригинальную прошивку TP-Link. Например:
    - Для US версии v4.6: `wpa8631pv4_us-up-ver4-0-2-build-20230825-rel...bin`
    - Для EU версии v3: может быть `wpa8631pv3_eu-up-...`
3.  **Повторите прошивку** с переименованным файлом.

---

## Шаг 7: Первый вход в OpenWrt (ПОСЛЕ прошивки) — РЕШЕНИЕ КОНФЛИКТА IP

### Проблема: после прошивки адаптер и ваш основной роутер снова конфликтуют

OpenWrt по умолчанию тоже использует IP-адрес `192.168.1.1`. Если ваш основной роутер уже работает на этом адресе, вы не сможете попасть в веб-интерфейс LuCI, не отключив роутер или не изменив настройки.

**У вас есть два способа. Начните со Способа 1 — он самый простой.**

### Способ 1 (Рекомендуемый): Временное отключение роутера

Это самый быстрый способ для первоначальной настройки.

1.  **Полностью отключите ваш основной роутер от питания.**
2.  **Убедитесь, что компьютер подключен напрямую** к LAN-порту адаптера TL-WPA8631P (как в Шаге 3).
3.  **На компьютере должен быть настроен статический IP** `192.168.1.10`, маска `255.255.255.0` (если вы уже настраивали — оставьте как есть).
4.  **Откройте браузер** и введите **`http://192.168.1.1`**.
5.  **Вы увидите веб-интерфейс OpenWrt (LuCI).** Пароля пока нет.
6.  **Сразу перейдите к настройке** (Шаг 8), чтобы изменить IP-адрес адаптера на свободный (например, `192.168.1.2`).
7.  **После изменения IP-адреса** вы можете снова включить ваш основной роутер. Адаптер теперь будет доступен по новому адресу.

### Способ 2 (Без отключения роутера): Ручное изменение IP на компьютере

Если вы не можете отключить роутер (например, из-за других пользователей в сети).

1.  **Отключите компьютер от основного роутера** (выньте кабель из LAN-порта или отключитесь от Wi-Fi).
2.  **Подключите компьютер напрямую** к адаптеру TL-WPA8631P патч-кордом.
3.  **Назначьте компьютеру статический IP** в другой подсети, отличной от `192.168.1.x`:
    - IP-адрес: `10.0.0.10`
    - Маска подсети: `255.255.255.0`
    - Шлюз: оставьте пустым
4.  **Откройте браузер** и введите адрес адаптера — он всё еще `192.168.1.1` (ваш компьютер теперь в сети `10.0.0.x`, но браузер всё равно сможет достучаться до `192.168.1.1`, так как они подключены напрямую).
5.  Вы попадете в LuCI. **Сразу измените IP-адрес адаптера** на свободный в вашей основной сети (например, `192.168.1.2`), как описано в Шаге 8.
6.  После этого подключите компьютер обратно к роутеру и заходите на адаптер по новому адресу (`192.168.1.2`).

---

## Шаг 8: Базовая настройка OpenWrt

### 8.1. Смена IP-адреса адаптера (чтобы не конфликтовать с роутером)

1.  В веб-интерфейсе LuCI перейдите в `Network → Interfaces`.
2.  Рядом с надписью `LAN` нажмите кнопку **`Edit`**.
3.  В разделе `IPv4 address` измените `192.168.1.1` на свободный адрес в сети вашего роутера. Например, если ваш роутер — `192.168.1.1`, то адаптеру дайте `192.168.1.2`.
4.  Нажмите **`Save & Apply`**.
5.  **После этого адаптер станет доступен по новому IP-адресу** (например, `192.168.1.2`). Подключите компьютер обратно к роутеру и используйте этот адрес для доступа.

### 8.2. Задание пароля администратора (ОБЯЗАТЕЛЬНО!)

1.  Перейдите в `System → Administration`.
2.  В поле "Router Password" введите пароль дважды.
3.  Нажмите **`Save & Apply`**.

### 8.3. Настройка Wi-Fi

1.  Перейдите в `Network → Wireless`.
2.  Вы увидите два радиоинтерфейса: `radio0` (2.4 ГГц) и `radio1` (5 ГГц).
3.  Для каждого нажмите **`Edit`**:
    - В поле `ESSID` введите имя вашей Wi-Fi сети.
    - В разделе `Wireless Security` выберите `WPA2-PSK` или `WPA3-SAE` и задайте пароль.
4.  Нажмите **`Save & Apply`** для каждого.

### 8.4. Установка пакетов (например, `open-plc-utils` для управления PowerLine)

Подключитесь к адаптеру по SSH или откройте вкладку `System → Software`.

**Через SSH (удобнее):**
```bash
ssh root@192.168.1.2   # используйте ваш новый IP-адрес
opkg update
opkg install open-plc-utils
```

Теперь доступны команды: `plcstat`, `plcinfo`, `plcdev` и другие.

**Для установки `zapret2`:**
```bash
opkg install zapret
```

---

## Шаг 9: Восстановление заводской прошивки (если понадобится)

Если что-то пошло не так или вы хотите вернуться к TP-Link.

### Метод: Через загрузчик TFTP

1.  Скачайте последнюю официальную прошивку TP-Link для вашей версии.
2.  Переименуйте её в специальное имя, которое требует загрузчик (обычно `tplink.bin` или `firmware.bin` — уточните для вашей модели).
3.  Настройте на компьютере статический IP `192.168.1.10`, маску `255.255.255.0`.
4.  Запустите TFTP-сервер на компьютере.
5.  Выключите адаптер из розетки, зажмите кнопку **Reset**, включите и держите, пока индикатор не начнёт быстро мигать (около 10 секунд).
6.  Адаптер сам заберёт файл с TFTP-сервера и прошьётся. Ждите 5-10 минут.

---

## Часто задаваемые вопросы (FAQ)

### ❓ Можно ли прошить v4 прошивкой от v3?

✅ **Да.** Это официально поддерживаемый способ. OpenWrt для TL-WPA8631P v4 не выпускает отдельный файл `factory.bin`, вместо этого образ для v3 работает и на v4.

### ❓ Будет ли работать PowerLine после прошивки?

✅ **Да.** Базовая функциональность HomePlug AV сохраняется. Для расширенного управления установите пакет `open-plc-utils`.

### ❓ Что будет, если залить `initramfs-kernel.bin` вместо `factory.bin`?

❌ Устройство загрузит OpenWrt в оперативную память, **но после перезагрузки всё вернётся к заводской прошивке**. Этот файл не прошивается в постоянную память.

### ❓ После прошивки не открывается веб-интерфейс. Что делать?

1.  Убедитесь, что вы разрешили конфликт IP (см. Шаг 7).
2.  Подключитесь по кабелю напрямую, без Wi-Fi.
3.  Попробуйте зайти по SSH: `ssh root@192.168.1.1` (или по новому адресу).
4.  Сбросьте настройки OpenWrt: зажмите кнопку Reset на 10 секунд при включении.

### ❓ Можно ли потом обновиться до более новой версии OpenWrt?

✅ **Да.** Используйте файл `...-sysupgrade.bin` из папки новой версии через веб-интерфейс OpenWrt (`System → Backup / Flash Firmware`).

### ❓ У меня v4.6 (или v4.60). Подойдёт ли прошивка от v3?

✅ **Да.** Все версии `v4.x` (включая `v4.6` и `v4.60`) используют один и тот же образ от `v3`. Разработчики OpenWrt не создают отдельный файл для каждой подверсии `v4`.

### ❓ Нужно ли обновлять стоковую прошивку TP-Link перед установкой OpenWrt?

⚠️ **Рекомендуется, но не обязательно.** Обновление прошивки TP-Link обновляет микрокод чипа PowerLine и загрузчика, что может предотвратить редкие проблемы с совместимостью. Если у вас уже стоит свежая версия — пропускайте.

---

## Поздравляю!

Вы успешно превратили обычный Powerline-адаптер в мощное сетевое устройство под вашим полным контролем.

**Что дальше?**
- Настройте единую mesh-сеть с вашим основным роутером (BPI-R4 / OpenWRT One)
- Установите `zapret2` для обхода DPI
- Запустите AdGuard Home для фильтрации рекламы
- Настройте WireGuard VPN
- Исследуйте возможности OpenWrt — их сотни!


